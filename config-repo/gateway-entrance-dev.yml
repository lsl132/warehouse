spring: 
  cloud:
    gateway:
      routes:
      - id: test
        uri: http://test.com/
        predicates:
        - Path=/test/**
      - id: mode
        uri: lb://mode-server
        predicates:
        - Path=/mode-server/**
